<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chỉnh Sửa Hồ Sơ - Hạt Đẻ Chill</title>
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
      rel="stylesheet"
    />
    <style>
      :root {
        --primary-color: #6366f1;
        --primary-dark: #4f46e5;
        --success-color: #10b981;
        --danger-color: #ef4444;
        --light-bg: #f8fafc;
        --border-color: #e2e8f0;
      }
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        min-height: 100vh;
        display: flex;
        justify-content: center; /* căn giữa ngang */
        align-items: center; /* căn giữa dọc */
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        margin: 0;
        padding: 0;
      }
      .profile-container {
        background: white;
        border-radius: 16px;
        box-shadow: 0 20px 60px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        max-width: 520px;
        width: 100%;
        animation: slideUp 0.5s ease-out;
      }
      @keyframes slideUp {
        from {
          opacity: 0;
          transform: translateY(30px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
      .profile-header {
        background: linear-gradient(
          135deg,
          var(--primary-color) 0%,
          var(--primary-dark) 100%
        );
        color: white;
        padding: 40px 30px;
        text-align: center;
        position: relative;
      }
      .profile-header::after {
        content: "";
        position: absolute;
        bottom: -1px;
        left: 0;
        right: 0;
        height: 20px;
        background: white;
        border-radius: 50% 50% 0 0 / 100% 100% 0 0;
      }
      .profile-header i {
        font-size: 48px;
        margin-bottom: 15px;
        display: block;
        opacity: 0.9;
      }
      .profile-header h1 {
        font-size: 28px;
        font-weight: 700;
        margin: 0;
        letter-spacing: -0.5px;
      }
      .profile-header p {
        margin: 8px 0 0 0;
        opacity: 0.9;
        font-size: 14px;
        font-weight: 500;
      }
      .profile-body {
        padding: 40px 30px;
      }
      .alert {
        border: none;
        border-radius: 10px;
        padding: 15px 20px;
        margin-bottom: 25px;
        font-size: 14px;
        animation: fadeIn 0.3s ease-in;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }
      .form-group {
        margin-bottom: 24px;
      }
      .form-group label {
        display: block;
        margin-bottom: 8px;
        font-weight: 600;
        color: #1e293b;
        font-size: 14px;
        letter-spacing: 0.3px;
      }
      .form-control {
        border: 2px solid var(--border-color);
        border-radius: 8px;
        padding: 12px 16px;
        font-size: 14px;
        transition: all 0.3s ease;
        background-color: var(--light-bg);
        color: #1e293b;
      }
      .form-control:focus {
        background-color: white;
        border-color: var(--primary-color);
        box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
        outline: none;
      }
      .form-control::placeholder {
        color: #94a3b8;
      }
      .password-hint {
        font-size: 12px;
        color: #64748b;
        margin-top: 6px;
        display: flex;
        align-items: center;
        gap: 6px;
      }
      .password-hint i {
        font-size: 11px;
        color: #94a3b8;
      }
      .btn-update {
        background: linear-gradient(
          135deg,
          var(--success-color) 0%,
          #059669 100%
        );
        border: none;
        color: white;
        padding: 12px 40px;
        font-weight: 600;
        border-radius: 8px;
        width: 100%;
        cursor: pointer;
        transition: all 0.3s ease;
        font-size: 15px;
        letter-spacing: 0.3px;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 8px;
      }
      .btn-update:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 20px rgba(16, 185, 129, 0.3);
      }
      .btn-update:active {
        transform: translateY(0);
      }
      .login-prompt {
        text-align: center;
        padding: 40px 20px;
      }
      .login-prompt p {
        color: #64748b;
        font-size: 15px;
        margin: 0;
      }
      .divider {
        height: 1px;
        background: var(--border-color);
        margin: 20px 0;
      }
      .form-section-title {
        font-size: 13px;
        font-weight: 700;
        color: #94a3b8;
        text-transform: uppercase;
        letter-spacing: 1px;
        margin-bottom: 16px;
        margin-top: 16px;
      }
      .form-section-title:first-child {
        margin-top: 0;
      }
/* Fix navbar và footer cho mobile 400px */
@media (max-width: 400px) {
  /* Navbar */
  .navbar {
    padding: 10px 12px !important;
  }

  .navbar .navbar-brand {
    font-size: 16px !important;
  }

  .navbar .navbar-brand img {
    width: 32px !important;
    height: 32px !important;
  }

  .navbar-toggler {
    padding: 4px 8px !important;
    font-size: 18px !important;
  }

  .navbar-collapse {
    margin-top: 10px !important;
  }

  .navbar-nav {
    gap: 8px !important;
  }

  .nav-link {
    padding: 8px 12px !important;
    font-size: 13px !important;
  }

  /* Footer */
  footer {
    padding: 15px 12px !important;
    font-size: 12px !important;
  }

  footer p {
    margin: 0 !important;
    line-height: 1.4 !important;
  }

  /* Container wrapper bên ngoài */
  body > div[style] {
    padding: 0 !important;
  }

  /* Profile container spacing */
  .profile-container {
    margin-top: 15px !important;
    margin-bottom: 15px !important;
  }

  /* Đảm bảo body có scroll khi cần */
  body {
    min-height: 100vh !important;
    height: auto !important;
    overflow-y: auto !important;
  }
}

/* Fix thêm cho 360px */
@media (max-width: 360px) {
  .navbar {
    padding: 8px 10px !important;
  }

  .navbar .navbar-brand {
    font-size: 14px !important;
  }

  .navbar .navbar-brand img {
    width: 28px !important;
    height: 28px !important;
  }

  footer {
    padding: 12px 10px !important;
    font-size: 11px !important;
  }

  .profile-container {
    margin-top: 12px !important;
    margin-bottom: 12px !important;
  }
}
    </style>
  </head>
  <body>
    <div
      style="
        display: flex;
        justify-content: center;
        align-items: center;
        width: 100%;
        height: 100%;
      "
    >
      <div class="profile-container">
             
        <div class="profile-header">
          <i class="fas fa-user-circle"></i>
          <h1>Hồ Sơ Cá Nhân</h1>
          <p>Cập nhật thông tin của bạn</p>
        </div>

           <% if (success && success.length > 0) { %>
          <div class="alert alert-success">
            <i class="fas fa-check-circle"></i>
            <span><%= success[0] %></span>
          </div>
        <% } %>
        <div class="profile-body">
          <% if (error) { %>
          <div class="alert alert-info">
            <i class="fas fa-info-circle"></i> <%= error %>
          </div>
          <% } %> <% if (user) { %>
          <form method="POST" action="/users/profile">
            <!-- Thông tin cơ bản -->
            <div class="form-section-title">
              <i class="fas fa-user"></i> Thông tin cơ bản
            </div>
            <div class="form-group">
              <label for="name">
                <i
                  class="fas fa-id-card"
                  style="color: var(--primary-color); margin-right: 6px"
                ></i
                >Tên
              </label>
              <input
                type="text"
                class="form-control"
                id="name"
                name="name"
                value="<%= user.name %>"
                placeholder="Nhập tên của bạn"
                required
              />
            </div>
            <div class="form-group">
              <label for="email">
                <i
                  class="fas fa-envelope"
                  style="color: var(--primary-color); margin-right: 6px"
                ></i
                >Email
              </label>
              <input
                type="email"
                class="form-control"
                id="email"
                name="email"
                value="<%= user.email %>"
                placeholder="your@email.com"
                required
              />
            </div>
            <!-- Bảo mật -->
            <div class="divider"></div>
            <div class="form-section-title">
              <i class="fas fa-lock"></i> Bảo mật
            </div>
            <div class="form-group">
              <label for="oldPassword">
                <i
                  class="fas fa-key"
                  style="color: var(--primary-color); margin-right: 6px"
                ></i
                >Mật khẩu hiện tại
              </label>
              <input
                type="password"
                class="form-control"
                id="oldPassword"
                name="oldPassword"
                placeholder="Nhập mật khẩu hiện tại"
                autocomplete="current-password"
              />
            </div>
            <div class="form-group">
              <label for="password">
                <i
                  class="fas fa-shield-alt"
                  style="color: var(--primary-color); margin-right: 6px"
                ></i
                >Mật khẩu mới
              </label>
              <input
                type="password"
                class="form-control"
                id="password"
                name="password"
                placeholder="Nhập mật khẩu mới"
                autocomplete="new-password"
              />
              <div class="password-hint">
                <i class="fas fa-info-circle"></i> Để trống nếu không muốn thay
                đổi (tối thiểu 6 ký tự)
              </div>
            </div>
            <!-- Nút cập nhật -->
            <button type="submit" class="btn-update">
              <i class="fas fa-check-circle"></i> Cập nhật hồ sơ
            </button>
          </form>
          <% } else { %>
          <div class="login-prompt">
            <i
              class="fas fa-lock"
              style="
                font-size: 40px;
                color: #cbd5e1;
                margin-bottom: 15px;
                display: block;
              "
            ></i>
            <p>Bạn cần đăng nhập để chỉnh sửa hồ sơ cá nhân</p>
          </div>
          <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
